#include "svq/gfx/utils.h"

namespace svq {
namespace gfx {


// Sprite sorting method to be used with std::sort
//inline bool compareSprites( const Sprite& a, const Sprite& b) {
	//if(a.m_tx.z_order == b.m_tx.z_order)
	//	return a.tex_id < b.tex_id;
	//else
	//	return a.m_tx.z_order < b.m_tx.z_order;
//	return 0;
//}

}
}